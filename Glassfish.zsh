# create_database(){#   applicationname=$(cat config/application.name)#   applicationName=gsed  -e 's/-\([a-z]\)/\u\1/g'  config/application.name###   ssh /bin/bash << EOF## cd /servers/glassfish/glassfish3/glassfish/bin# ./asadmin login# if [ $? != 0 ]; then#    echo "Must login to continue"#    exit# fi# ￼# ./asadmin create-jdbc-connection-pool \#     --datasourceclassname=org.postgresql.ds.PGConnectionPoolDataSource \#     --restype=javax.sql.ConnectionPoolDataSource \#     --property user=${DB_USER}:password=${DB_PASSWORD}:databaseName=${DB}:se#     rverName=${DB_HOST} \#     ${JDBC_POOL}# ./asadmin create-jdbc-resource \#     --connectionpoolid ${JDBC_POOL} \#     ${JDBC_RESOURCE}# EOF# }function start_glassfish(){ G start-domain --verbose 1>/dev/null 2>&1 &}